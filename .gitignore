# Dependencies
/node_modules

# Build output
/dist
/coverage
*.tsbuildinfo

# Secrets / local env (never commit private keys)
.env
.env.*
!.env.example

# Local deploy scripts with SSH credentials (never commit)
deploy/remote_deploy.py
deploy/redeploy.py
deploy/*.local.py

# Logs
*.log
logs

# OS / editor
.DS_Store
.idea
.vscode
